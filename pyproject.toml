[project]
name = "job-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David", email = "david.huizinga@veneficus.nl" }
]
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0",
    "pydantic-ai>=1.0",
    "pypdf>=5.0",
]

[project.scripts]
job-search = "job_search:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
